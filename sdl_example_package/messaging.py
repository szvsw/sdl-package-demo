def say_hello(name):
	if (name):
		print(f'Hello world (and you too, {name})!')
	else:
		print("Hello world!")

def say_goodbye(name):
	if (name):
		print(f'Goodbye world (and you too, {name})!')
	else:
		print("Goodbye world!")